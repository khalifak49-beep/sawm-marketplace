# ميزة: فروع الشركات + حدود المزايدة بموافقة الرئيسية

> ✅ **مكتملة ومختبَرة** (Playwright، 8 سيناريوهات نجحت جميعها بلا أخطاء).

## إخفاء هوية المزايدين (مزاد سرّي)
في جدول مزايدات المزاد، هوية المزايد محجوبة ("🔒 مزايد ١ · هوية محجوبة") للجميع، وتُكشف فقط لـ:
- **الإدارة** — كل الهويات.
- **الشركة الأم** — هويات فروعها فقط (لا المنافسين، ولا يرى فرعٌ فرعاً شقيقاً).
- **كل مزايد** — صفّه هو (يظهر "أنت").

المزارع والوسيط يرسّيان بالسعر والترتيب دون معرفة الهوية؛ وتُكشف الهوية تلقائياً عند الترسية
(يصير الطرفان طرفَي عقد). الأسعار والترتيب تبقى ظاهرة — تُخفى الهوية فقط.
مُطبّق في `AuctionsController.Details` + `Views/Auctions/Details.cshtml`. مختبَر على 7 أنواع مشاهدين.

## إخفاء أطراف المزاد + وحدة الطن
- **بطاقة "أطراف المزاد"** (هوية المزارع والوسيط) تظهر **للإدارة فقط**؛ يرى غيرُهم لافتة
  "🔒 هويات أطراف المزاد محجوبة … تُكشف عند الترسية". (`Views/Auctions/Details.cshtml`)
- **شاشة إنشاء المزاد:** الكمية تُدخل **بالطن** (لاحقة "طن" + نص "أدخل الكمية بالطن، ١ طن = ١٠٠٠ كجم")،
  والسعر "ر.ع / طن". كل مزاد جديد يُخزَّن بـ `QuantityUnit = "طن"` ويُعرض بالطن.
- المزادات القديمة (`QuantityUnit = null`) تُعرض بوحدة محصولها كما كانت — بلا كسر. مختبَر.

## إضافة محصول جديد من شاشة إنشاء المزاد
قائمة المحصول فيها خيار **"➕ أخرى — إضافة محصول جديد"** (`CropId = -1`). عند اختياره يظهر حقل
"اسم المحصول الجديد". عند النشر يُنشأ المحصول (وحدة الطن، فئة "أخرى") ويُربط بالمزاد ويصبح متاحاً
للجميع في القائمة. اسم موجود مسبقاً يُعاد استخدامه بدل التكرار. التحقق يمنع "أخرى" بلا اسم.
(`AuctionsController.Create` + `Views/Auctions/Create.cshtml`). مختبَر (5 حالات).

## اعتماد المزادات من الإدارة (تدقيق)
- كل مزاد جديد (من مزارع أو وسيط) يُنشأ بحالة **"بانتظار اعتماد الإدارة"** — لا يُفتح للمزايدة تلقائياً.
- **الإدارة** فقط تعتمد (`Approve` → نشط) أو ترفض (`Reject` → ملغى بسبب)، ويمكنها **تعديل كل تفاصيل المزاد**
  قبل الاعتماد عبر شاشة `Auctions/Edit` (العنوان، المحصول، الكمية، السعر، التواريخ، الوسيط، إلخ).
- **الوسيط** يستطيع "تبنّي الإشراف" على مزاد بلا وسيط (`Adopt`) — يبقى بانتظار اعتماد الإدارة.
- طابور `Admin/PendingAuctions` + عنصر تنقّل "اعتماد المزادات" بعدّاد. مختبَر (5 مراحل) بلا أخطاء.

## للتجربة
- **الشركة الرئيسية:** `company1@sawm.om` — قسم "إدارة الشركة" في الشريط الجانبي:
  - **الفروع:** إنشاء فرع + ضبط حد المزايدة والصلاحيات.
  - **نشاط الفروع:** طابور موافقات المزايدات المتجاوزة للحد + كل مزايدات الفروع (فروعها فقط).
- **فرع تجريبي (حد 3000):** `branch.muscat@sawm.om` — لا يطرح مناقصات، لا يوقّع عقوداً.
- **فرع تجريبي (حد 6000، كل الصلاحيات):** `branch.salalah@sawm.om`.
- كلمة المرور الموحدة: `Sawm@2026`.


## القرارات المعتمدة (من المستخدم)

1. **إضافة الفروع:** كل شركة رئيسية تُنشئ حسابات فروع لها (بريد + كلمة مرور) وتمنح كل فرع صلاحيات.
2. **تجاوز الحد → موافقة الرئيسية:** عندما تتجاوز مزايدة الفرع الحدَّ الذي حددته الرئيسية،
   تُعلَّق المزايدة بحالة "بانتظار موافقة الرئيسية" ولا تدخل المزاد فعلياً حتى توافق الرئيسية.
   الرئيسية ترى الفرع والمبلغ والمزاد وتوافق أو ترفض.
3. **نطاق الحد:** القيمة الإجمالية للمزايدة (سعر الوحدة × الكمية) = التعرّض المالي الفعلي.
   الحد `<= 0` يعني "بلا حد" (الفرع يزايد بحرية). قيمة موجبة = السقف المسموح دون موافقة.
4. **الرؤية مقيّدة:** الشركة الرئيسية ترى **فروعها فقط** التي دخلت المزاد — لا ترى مزايدات
   الشركات المنافسة الأخرى إطلاقاً.
5. **الصلاحيات القابلة للمنح لكل فرع:** المزايدة في المزادات · تقديم عروض على المناقصات ·
   طرح مناقصات · توقيع/إدارة العقود. (المستخدم أكّد "طرح مناقصات"؛ نجعلها كلها قابلة للتبديل.)

## نموذج البيانات

`CompanyProfile` يُضاف إليه:
- `ParentCompanyId` (string?, FK → AspNetUsers) — null = شركة رئيسية/مستقلة، غير null = فرع.
- `CanBid`, `CanSubmitOffers`, `CanCreateTenders`, `CanManageContracts` (bool).
- `BidLimit` (decimal) — سقف القيمة الإجمالية للمزايدة دون موافقة؛ `<= 0` = بلا حد.

`Bid`:
- `BidStatus.PendingApproval` (حالة جديدة) — تجاوزت حد الفرع وتنتظر موافقة الرئيسية.
- المزايدة المعلّقة لا تُحتسب في السعر الحالي ولا في الفائز حتى تُعتمد.

## المسارات
- `CompanyController`: Branches (قائمة), CreateBranch, EditBranch, BranchActivity (نشاط الفروع + طابور الموافقات), ApproveBid, RejectBid.
- `AuctionsController.Bid`: يفحص إن كان المزايد فرعاً → يطبّق الصلاحية والحد → تعليق/تسجيل.
- الشريط الجانبي: عنصر "الفروع" يظهر للشركة الرئيسية فقط.
- البذور: منح company1 فرعين للعرض.
